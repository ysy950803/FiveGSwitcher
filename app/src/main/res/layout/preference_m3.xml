<?xml version="1.0" encoding="utf-8"?>
<!--
    Material 3 preference row: a rounded surface "card" with generous spacing and
    M3 typography. Keeps the framework view ids (@android:id/icon, icon_frame,
    @android:id/title, @android:id/summary, @android:id/widget_frame) so every
    Preference type binds exactly like the default layout.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/m3_pref_card_margin_horizontal"
    android:layout_marginEnd="@dimen/m3_pref_card_margin_horizontal"
    android:layout_marginBottom="@dimen/m3_pref_item_gap"
    android:background="@drawable/bg_preference_item_m3"
    android:baselineAligned="false"
    android:gravity="center_vertical"
    android:minHeight="@dimen/m3_pref_item_min_height"
    android:paddingStart="@dimen/m3_pref_item_padding_horizontal"
    android:paddingEnd="@dimen/m3_pref_item_padding_horizontal"
    android:paddingTop="@dimen/m3_pref_item_padding_vertical"
    android:paddingBottom="@dimen/m3_pref_item_padding_vertical">

    <LinearLayout
        android:id="@+id/icon_frame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="start|center_vertical"
        android:minWidth="48dp"
        android:orientation="horizontal"
        android:paddingEnd="12dp"
        android:paddingRight="12dp">

        <ImageView
            android:id="@android:id/icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:maxWidth="40dp"
            android:maxHeight="40dp" />

    </LinearLayout>

    <RelativeLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <TextView
            android:id="@android:id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:textColor="@color/preference_title_m3" />

        <TextView
            android:id="@android:id/summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@android:id/title"
            android:layout_alignStart="@android:id/title"
            android:layout_alignLeft="@android:id/title"
            android:layout_marginTop="2dp"
            android:maxLines="10"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/preference_summary_m3" />

    </RelativeLayout>

    <!-- Preference widget (switch, etc.) is inflated here. -->
    <LinearLayout
        android:id="@android:id/widget_frame"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:gravity="end|center_vertical"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingLeft="16dp" />

</LinearLayout>
